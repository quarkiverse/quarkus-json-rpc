---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: JSON-RPC
description: Expose CDI bean methods as JSON-RPC 2.0 endpoints over WebSocket using the @JsonRPCApi annotation.
metadata:
  keywords:
    - json-rpc
    - jsonrpc
    - websocket
    - rpc
  categories:
    - "web"
  status: "preview"
